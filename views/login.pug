html
    head
        title zdc login page
        meta(name='viewport',content="width=device-width")
    body
        style.
            body {
                position: fixed;
                top: 0;
                left: 0;
                right: 0;
                bottom: 0;
                display: flex;
                align-items: center;
                justify-content: center;
                font-family: 'FiraSans', sans-serif;
                font-size: 120%;
                font-weight: 300;
                color: #427082;
            }

            form {
                display: flex;
                flex-direction: column;
            }

            p {
                margin: 0;
                padding: 0.6em 0;
                text-align: center;
            }

            label {
                display: flex;
                margin: 1em 0;
                align-items: center;
                flex-wrap: wrap;
            }

            label > span {
                flex-basis: 20%;
                padding: 0.5em 0;
            }

            input, button {
                outline: none;
            }

            input {
                flex-grow: 1;
                padding: 0.4em;
                font-size: 1em;
                border: 1px solid lightgrey;
            }

            input:focus, button:focus {
                border: 1px solid #52b49a;
                box-shadow: 0 0 8px #52b49a;
            }

            button {
                border: 1px solid #477485;
                align-self: center;
                padding: 0.5em;
                cursor: pointer;
                background: #477485;
                color: white;
                border-radius: 3px;
                font-size: 1em;
                box-shadow: 1px 1px 3px #427082;
            }

            button:hover {
                background: #4e7e90;
            }

            @media (max-width: 780px) {
                body {
                    font-size: 100%;
                    position: relative;
                    align-items: flex-start;
                }

                input {
                    flex-basis: 100%;
                }

                button {
                    align-self: stretch;
                }
            }

        form(method='POST', action='/magicLinks')
            p Send a valid email so we can provide you with a magic link to login into zdc
            input(hidden, name='grantId', value=grant.id)
            label
                span Email
                input(type='email',autofocus, name='email', required, placeholder='hello@example.com', autocomplete='email')
            button Send Magic Link